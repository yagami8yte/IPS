<UserControl x:Class="IPS.MainApp.Views.Controls.NumericKeypad"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="400">
    <Grid>
        <!-- Numeric Keypad Layout (3x4 grid + bottom row) -->
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Number Buttons Style -->
        <Grid.Resources>
            <Style x:Key="KeypadButtonStyle" TargetType="Button">
                <Setter Property="FontSize" Value="48"/>
                <Setter Property="FontWeight" Value="Bold"/>
                <Setter Property="FontFamily" Value="{StaticResource BrandFont}"/>
                <Setter Property="Background" Value="{StaticResource BackgroundBeige}"/>
                <Setter Property="Foreground" Value="{StaticResource DarkBrown}"/>
                <Setter Property="BorderBrush" Value="{StaticResource AccentGold}"/>
                <Setter Property="BorderThickness" Value="2"/>
                <Setter Property="Margin" Value="8"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="border"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="15">
                                <ContentPresenter HorizontalAlignment="Center"
                                                VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="border" Property="Background" Value="{StaticResource AccentLatte}"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style x:Key="ActionButtonStyle" TargetType="Button" BasedOn="{StaticResource KeypadButtonStyle}">
                <Setter Property="FontSize" Value="32"/>
                <Setter Property="Background" Value="{StaticResource MediumBrown}"/>
                <Setter Property="Foreground" Value="{StaticResource LightCream}"/>
                <Setter Property="BorderBrush" Value="{StaticResource DarkBrown}"/>
            </Style>
        </Grid.Resources>

        <!-- Row 1: 1, 2, 3 -->
        <Button Grid.Row="0" Grid.Column="0" Content="1" Style="{StaticResource KeypadButtonStyle}" Click="Number_Click"/>
        <Button Grid.Row="0" Grid.Column="1" Content="2" Style="{StaticResource KeypadButtonStyle}" Click="Number_Click"/>
        <Button Grid.Row="0" Grid.Column="2" Content="3" Style="{StaticResource KeypadButtonStyle}" Click="Number_Click"/>

        <!-- Row 2: 4, 5, 6 -->
        <Button Grid.Row="1" Grid.Column="0" Content="4" Style="{StaticResource KeypadButtonStyle}" Click="Number_Click"/>
        <Button Grid.Row="1" Grid.Column="1" Content="5" Style="{StaticResource KeypadButtonStyle}" Click="Number_Click"/>
        <Button Grid.Row="1" Grid.Column="2" Content="6" Style="{StaticResource KeypadButtonStyle}" Click="Number_Click"/>

        <!-- Row 3: 7, 8, 9 -->
        <Button Grid.Row="2" Grid.Column="0" Content="7" Style="{StaticResource KeypadButtonStyle}" Click="Number_Click"/>
        <Button Grid.Row="2" Grid.Column="1" Content="8" Style="{StaticResource KeypadButtonStyle}" Click="Number_Click"/>
        <Button Grid.Row="2" Grid.Column="2" Content="9" Style="{StaticResource KeypadButtonStyle}" Click="Number_Click"/>

        <!-- Row 4: Clear, 0, Backspace -->
        <Button Grid.Row="3" Grid.Column="0" Content="Clear" Style="{StaticResource ActionButtonStyle}" Click="Clear_Click"/>
        <Button Grid.Row="3" Grid.Column="1" Content="0" Style="{StaticResource KeypadButtonStyle}" Click="Number_Click"/>
        <Button Grid.Row="3" Grid.Column="2" Content="⌫" Style="{StaticResource ActionButtonStyle}" Click="Backspace_Click"/>
    </Grid>
</UserControl>
